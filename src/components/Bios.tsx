import React, { FC } from "react";
import Avatar from "./Avatar";

const Bios: FC = () => {
  return (
    <div className="container" style={{ width: "100%" }}>
      <Avatar />
    </div>
  );
};

export default Bios;
